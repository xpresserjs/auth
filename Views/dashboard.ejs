<%- include('partials/header') %>
<div class="row">
    <div class="col-12 col-md-8 offset-md-2 col-lg-6 offset-lg-3 col-xl-4 offset-xl-4">
        <div class="card shadow">
            <div class="card-body">
                <h2 class="brand text-center mb-5">Xpresser<span class="text-danger">js</span></h2>
                <h4>Dashboard</h4>
                <hr class="m-0 border-danger">

                <%- include('partials/alerts') %>

                <pre class="mt-4 mb-5 bg-light p-3 rounded"><%= JSON.stringify(user, null, 2) %></pre>

                <form action="<%=ctx.route('auth.logout')%>" method="post" class="text-center mt-5">
                    <button type="submit" class="btn btn-block btn-danger">
                        Logout
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
<%- include('partials/footer') %>
